{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm-install-sandbox",
      "type": "shell",
      "command": "mise exec -- npm install",
      "options": {
        "cwd": "${workspaceFolder}/sandbox"
      }
    },
    {
      "label": "wait-for-sandbox-server",
      "type": "shell",
      "command": "while ! curl -s http://localhost:3000 > /dev/null 2>&1; do echo 'Waiting for http://localhost:3000 ...'; sleep 1; done"
    }
  ]
}
